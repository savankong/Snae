import { CreatorOnboarding } from '@/components/creator-onboarding';

export const metadata = {
  title: 'Apply to create',
  description: 'Join Snae as a verified creator. Keep up to 85%, get paid weekly, and work behind real protection.',
};

export default function CreatorOnboardingPage() {
  return <CreatorOnboarding />;
}
